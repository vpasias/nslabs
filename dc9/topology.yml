#
# EVPN
#
provider: clab

name: dc

defaults:
 device: srlinux

bgp.as_list:
  65000:
    members: [ s1, s2, l1, l2 ]
    rr: [ s1, s2 ]
    
groups:
  as65000:
    node_data:
      bgp.advertise_loopback: True
  hosts:
    members: [ h1, h2 ]

nodes:
  s1:
    module: [ bgp, evpn, bfd ]
  s2:
    module: [ bgp, evpn, bfd ]
  l1:
    module: [ bgp, evpn, bfd ]
  l2:
    module: [ bgp, evpn, bfd ]
  h1:
    device: linux
    module: []
  h2:
    device: linux
    module: []

links: [ s1-l1, s1-l2, s2-l1, s2-l2, l1-h1, l2-h2 ]
